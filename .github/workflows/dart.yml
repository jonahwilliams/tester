name: Main

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build_linux:
    runs-on: ubuntu-latest
    container:
      image:  google/dart:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install dependencies
        run: pub get
      - name: Build
        run: dart2native -o linux/tester bin/tester.dart
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: linux/tester
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}    
  build_windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup
        uses: DanTup/gh-actions/setup-dart@master
      - name: Install dependencies
        run: pub get
      - name: Build
        run: dart2native -o windows/tester.exe bin/tester.dart
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: windows/tester.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  build_macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup
        uses: DanTup/gh-actions/setup-dart@master
      - name: Install dependencies
        run: pub get
      - name: Builds
        run: dart2native -o macos/tester bin/tester.dart
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: macos/tester
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
